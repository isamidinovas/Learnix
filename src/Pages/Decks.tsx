import React from "react";
import Layout from "../Layouts/Layout";

import DecksContainer from "../components/DecksPage/DecksContainer";

const Decks: React.FC = () => {
  return (
    <Layout>
      <DecksContainer />
    </Layout>
  );
};

export default Decks;
